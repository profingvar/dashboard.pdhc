Current focus: #462 clinical-dashboard redesign (D3+D5 landed 2026-07-13).

Decisions + boundary: docs/redesign_462_decisions.md (operator #469 answers).
Tickets: #463 D1, #464 D2, #465 D3(done), #466 D4, #467 D5(done),
#468 D6(cdr.pdhc), #469 open-questions.

DONE this session (local, tested, 222/222 green — NOT deployed):
  - #467 D5 SavedDesign — /api/v1/designs CRUD, owner-private reusable
    template, migration sd071322aa01 (single head).
  - #465 D3 patient picker — GET /select, org-affiliation scoped, reads
    CDR1 via app/services/cdr1_client.py (care-delivery basis).
  - #468 D6 (cdr.pdhc) — /api/v1/clinical care-delivery read surface
    (patients-with-data index + per-patient concept summary).
  - #463 D1 — route-aware SSO gate: clinical routes → care-delivery,
    analyse routes keep analysis-phase (app/auth.py). Boundary inventory
    in docs/redesign_462_decisions.md §D1.
  - #470 (new) — physical relocation of the analyse engine to analyse.pdhc
    (deferred; blocked until analyse.pdhc exists).
  - #464 D2 — CDR1-backed per-patient reads: cdr1_client.patient_series +
    app/routes/charts.py proxies (parameters/series) with spärr. CDR side:
    /api/v1/clinical/patient/<guid>/series.
  - #466 D4 — charts page /patient/<guid>/charts (charts.html): ≤3 diagrams,
    dual-axis mirror, continuous time slider, zero/y-max, save/load designs.
  - #471 (new) — cutover follow-up: retire legacy /patient+ObservationCache,
    re-home #212, vendor Chart.js, spärr lift refinement, plan display names.

Q1 (legal basis) CONFIRMED: care-delivery, dashboard declares purpose →
CDR1 skips #422 analysis-consent; spärr via existing ips_client; vård-
relation model → legal (#437). Q6 (cache) still open.

Next / blocked:
  1. #468 (cdr.pdhc, D6) — CDR1 must honour X-Access-Purpose:care-delivery
     (skip check_patient_allowed) AND expose a per-org patient-index +
     per-patient concept counts. UNBLOCKS the picker's "has data" +
     volume sort AND #464's per-patient reads. This is the real gate now.
  2. #463 (D1) — move dashboard off the analysis phase gate onto care-
     access; relocate the analyse/federation engine toward analyse.pdhc.
  3. #464 (D2) per-patient CDR1 reads; #466 (D4) charts.

Live smoke of /select against CDR1 deferred until: tunnel up + #468 done +
CDR1_BASE_URL/DASHBOARD_PDHC_SERVICE_KEY set on the server.

Legacy items (pre-#462, still open, none blocking):
  - services.html false-greens (www.pdhc.se polling JS).
  - /healthz DB ping standardisation on contract.pdhc / plan.pdhc.
